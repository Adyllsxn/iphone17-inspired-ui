###############################################################################
# Normalização de final de linha (EOL)
###############################################################################

# Normaliza os finais de linha automaticamente com base no SO
* text=auto

###############################################################################
# Tratamento de arquivos de solução e projeto do Visual Studio
###############################################################################

# Arquivos de projeto são tratados como binários
# Isso evita conflitos automáticos que podem corromper os arquivos
*.sln       merge=binary
*.csproj    merge=binary
*.vbproj    merge=binary
*.fsproj    merge=binary

###############################################################################
# Tratamento de arquivos de imagem
###############################################################################

# Garante que imagens sejam tratadas como binários e não sejam mescladas ou analisadas como texto
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.svg       binary

###############################################################################
# Configurações de diff opcionais para documentos (ativar se necessário)
###############################################################################

# Exibe conteúdo como texto ao fazer diff em arquivos binários
# Requer configuração extra do Git para funcionar corretamente
#*.docx      diff=astextplain
#*.pdf       diff=astextplain